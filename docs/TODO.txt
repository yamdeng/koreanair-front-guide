1.publish aviation 반영

2.yup 정리

3.file 삭제시 목록 refresh 않된 현상 확인

4.autosize 전략 확인 

5.로그인 및 appStore 사용 가이드

6.상수
 -FileAttach mode 상수로 분류

7.yarn lint 전달

8.zustand store 파일 사용법 가이드
 -store readonly 되는 부분 확인

9.a href prevent event

10.에디터 마무리
 -이미지 hook
 -copy & paste
 -editor disabled 않먹음
 -viewer 확인

11.Logger 및 console 모두 제거

12.테이블 전체 확인

99.관리자 기능 개발



/* zustand store 생성 */
const useSysMessageFormStore = create((set, get) => {
  // 생성자와 유사한 초기화 로직
  const initializeState = () => {
    // 초기화할 상태나 로직 작성
    set({ count: 0, initialized: true });
  };

  debugger;
  // 초기화 로직 실행
  initializeState();

  return {
    ...createFormSliceYup(set, get),

    ...initFormData,

    yupFormSchema: yupFormSchema,

    clear: () => {
      set(initFormData);
    },
  };
});
