1.상위에 폼이 하나 존재하고

2.테이블이 존재함

3.마지막 자식에 폼이 하나 존재함
 -단 이 자식은 목록 기준을 따라감



changeInput: (inputName, inputValue) => {
  // TODO formValue set하는 것을 _.set을 사용해서 동작하는지 확인하기
}


getApiParam ===> 동일

validate ===> 동일

save ===> 동일

getDetail ===> TODO formValue에 특정 목록의 status를 'N'으로 않해도 될듯(없으면 기본 'N'으로 인식)

저장은 의미가 없고 validate만 의미 있어 보임

selectedListIndex: 0(기본값)

changeSelectedListIndex: (index) => {

},

changeListFormInput: (inputName, inputValue) => {
  changeInput() ===> 기존것 호출하고
  updaetStats(selectedIndex, 'U', true);
},

addRow: () => {
  selectedIndex = 0;
},

updateInfo: (index, newInfo) => {

},

updateStatus: (index, status, byPassBeforeStatus = false) => {

  // 기존에 status가 존재하지 않으면 'U'로 반영

  // 기존에 status가 'A'이면 바로 삭제시킴

  // 기존에 status가 'D'이면 반영하지 않음  

},

==========================================================================================


### bat 파일 ###

1.css 프로젝트는 1시간 마다 변경 이력이 존재하면 최신화 시키기게끔 했음 : css-start.bat
 -local.note:61000

2.관리자 화면 : 않띄워도 됩니다. : admin-front-start.bat
 -local:noteboot:8082
 -관리자만 해시라우터를 적용해서 메뉴 이동되도 동작되게끔 설정

3.admin 서버 프로젝트 : admin-back-start.bat
 -local.notebook:8084

4.generate-server : front-generate-start.bat
 -local.noteboot:3010





1.yarn lint 한꺼번에 확인
2.<ErrorBoundary/>
3.전역로그 정리



#.관리자 기능 개발 fix

#.로그 정리
 -Logger 및 console 모두 제거
 -경고문 전체 제거
 -a href prevent event



1.css 공통 컴포넌트 포팅
 -작업한 부분을 확인하기
 -viewer
 -fileattach 관련 사항 전달

2.전체 자동화 완성하기



2.상수 사용방법 확인
 -FileAttach mode 상수로 분류

5.에디터 마무리
 -이미지 hook
 -copy & paste
 -editor disabled 않먹음
 -viewer 확인




4.autosize 전략 확인

8.zustand store 파일 사용법 가이드
 -store readonly 되는 부분 확인

12.테이블 전체 확인

99.관리자 기능 개발

#.가이드 변경사항


